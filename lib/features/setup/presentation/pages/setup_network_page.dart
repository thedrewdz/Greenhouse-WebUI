import 'package:flutter/material.dart';

class SetupNetworkPage extends StatelessWidget {
  const SetupNetworkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Setup — Network Connection')),
    );
  }
}
