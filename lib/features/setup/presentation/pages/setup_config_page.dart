import 'package:flutter/material.dart';

class SetupConfigPage extends StatelessWidget {
  const SetupConfigPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Setup — Main Config')),
    );
  }
}
